- 为发布 GitHub 补齐仓库基础设施：新增 `.gitignore`（排除 Python 缓存、生成图、虚拟环境等）、`LICENSE`（MIT，署名 vaxicy，2026）、`requirements.txt`（Pillow>=10.0.0）。
- 将 `README.md` 重写为中英双语版本，包含功能说明、用法、CodeBuddy Skill 安装方式、视觉 QA 标准、项目示例和许可证信息。
- 本地提交：`docs: bilingual README, add MIT license, .gitignore and requirements.txt`。
- GitHub 用户名 `vaxicy`。已用 gh CLI（已登录，含 repo 权限）创建公开仓库 https://github.com/vaxicy/store-asset-generator，本地分支改名 master→main 并推送成功。
- 环境事实：该机器装有 gh CLI 2.89.0，已登录 GitHub 账号 vaxicy，可直接用 gh 建仓/推送。
- 仓库已重命名为 `chrome-extension-store-asset-generator`，GitHub 描述改为中英双语，并添加 10 个 SEO topics（chrome-extension、chrome-web-store、screenshot-generator 等）。
- SKILL.md 的 `name` 字段同步改为 `chrome-extension-store-asset-generator`，README 标题和安装路径全部更新。
- 新增 `examples/` 目录，从 `color-picker/store-assets` 和 `MiniCalc/theme-store/screenshots` 复制 4 张代表性效果图（Pixel Color Picker 宣传图+截图、MiniCalc 宣传图+截图）到仓库，用于 README 预览。
- README 新增「效果预览 | Preview」小节，使用 `<img>` 标签控制展示尺寸，并注明图片基于实际扩展 UI 风格与功能生成、通过视觉 QA。
- 调整 `.gitignore`：删除全局 `*.png/*.jpg` 忽略，改为仅忽略根目录 `/store-assets/`、`/screenshots/`、`/output/` 生成目录，使 `examples/` 图片能被 Git 追踪。
- 提交：`docs: add real-world example previews from Pixel Color Picker and MiniCalc`。
- 注意：因当前环境无法连接 `github.com:443`（Recv failure / Could not connect to server），该提交暂未能 push 到远程，需等网络恢复后手动运行 `git push`。
